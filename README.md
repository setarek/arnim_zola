# arnim_zola

How get best profit for itraday trading base on pivot points.
```cassandraql
curl --location --request POST 'localhost:9001/api/v1/spot/kline' \
--header 'Content-Type: application/json' \
--data-raw '{
    "symbol": "BTCUSDT"
}'
```